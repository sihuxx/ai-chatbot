# ── 환경 변수 ──
.env
.env.local
.env.production

# ── 빌드 결과물 ──
.next/
out/
build/

# ── 의존성 ──
node_modules/

# ── DB (로컬 SQLite) ──
prisma/*.db
prisma/*.db-journal

# ── 시스템 파일 ──
.DS_Store
Thumbs.db
desktop.ini

# ── 로그 ──
npm-debug.log*
yarn-debug.log*
*.log

# ── 에디터 ──
.vscode/
.idea/
*.swp
